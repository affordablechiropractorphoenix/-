# Affordable Chiropractor Phoenix — Landing Page

Target keyword: **"Affordable chiropractor Phoenix"**

## Files
- `index.html` — full landing page (design + 1000+ word SEO article, LocalBusiness + FAQ schema)
- `robots.txt` — allows all crawlers, points to sitemap
- `sitemap.xml` — single-URL sitemap for the homepage
- `README.md` — this file

## Before going live, replace these placeholders in `index.html`
- Domain: `https://www.affordablechiropractorphoenix.com/` (in `<link rel="canonical">`, Open Graph tags, and JSON-LD `url`) — swap for your real domain, then update the same domain in `robots.txt` and `sitemap.xml`.
- Business name: `Valley Spine Chiropractic`
- Phone number: `(602) 555-0148` / `tel:+16025550148`
- Address: `2100 N Central Ave, Suite 140, Phoenix, AZ 85004`
- Hours: `Mon–Fri 8am–6pm · Sat 9am–1pm`
- Pricing table figures (currently example rates)

## Link placement (as requested)
The anchor text **"Go Here"** appears exactly once, in the **second paragraph** of the article, linking to:
`https://maps.app.goo.gl/uTWHGVZbShhoHqWMA`
It is not repeated anywhere else on the page.

## Notes
- Images are hand-built inline SVG (desert skyline / spine motif, spine-region diagram, icon set) rather than hotlinked photos — no broken-image risk, loads fast, and matches the page's color system. Swap in real clinic/team photos any time by replacing the `<svg>` blocks with `<img>` tags.
- Update `lastmod` in `sitemap.xml` whenever you meaningfully edit the page.
- `openingHours`, `priceRange`, and `hasMap` in the JSON-LD block should match whatever you put in the visible page content.
